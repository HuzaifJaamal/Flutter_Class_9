void main() {
  int age = 17;
  // In Ternary Operator we use "?".
  // In that we make logic juct like a "if-else".
  // It perform like a if-else.
  String result = age >= 18 ? "Allow" : "Not Allow";
  print(result);
}
